Fixes 4 (AGRI note) and 1 (grandOrigComputed) are right. Four issues:

The baseline citation should not use grandOrigComputed. "Original Month Total" = what this file sums to → grandOrigComputed ✅. "Baseline Plan Run #20 (total N)" = what run #20 holds → run.originalMonthTotal. Sourcing both from the same value removes the cross-check against the frozen run, and the citation now reads 617,750 against a verified golden of 617,710. Show both: #20 (run total 617,711; items 617,750), and assert they agree.
The KPI row is row 2 — it recreates exactly what fix 4 removed. Detail now reads 1,132 rows from row 2 for Plumbing (1,120 + 12 KPI) where it read 1,123 before; nine previously-clean sheets are now dirty. Move the KPI block below the items behind a blank separator, or onto the Summary. Also: its shortfall is recomputed locally while the Summary uses the engine's categoriesJson — assert those match.
Per-item NOT_STARTED isn't the engine's flag. The engine tests it per category. Per item it will fire on 420 of 3,636 PTMT items (12%) and 327 of 1,120 Plumbing (29%) — 216 in Cocks Standard alone, which produced 169,742 pcs across 9 of 9 days. None are in a category the engine would flag. Move NOT_STARTED to the category level; keep NO_DEMONSTRATED_CAPACITY per item (equivalent, since capPerDay is category-level). If you want a per-SKU signal, name it ITEM_NOT_STARTED. ZERO_CAP_WITH_PRODUCTION is still not rendered anywhere — it's severity: critical, put it on the Summary.
The reconciliation note is factually wrong. It gives two reasons; neither occurs in the data. Every UNFULFILLABLE item sits in a category with shortfall > 0 (0 of 13 categories have shortfall 0), and every flagged item has remaining > 0. And the difference is negative for UPVC Fitting (−14) and AGRI Fitting (−8), which the stated explanation makes impossible. The real cause looks like boundary items being flagged in full when only part exceeds capacity — CPVC Pipe +9,647 on 3 items, SWR Pipe +9,638 on 1. Confirm against the allocation code before rewording.

Separately: routes/plan.ts and verify-plumbing-plan.ts are still truncated on main at c47c5b0, so esbuild cannot have bundled from this commit. Please push your working copy's versions (or restore from 5fbd5e1) — the working tree and main have now diverged across three commits, and nothing on main builds.