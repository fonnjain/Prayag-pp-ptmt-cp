# Replit Prompt — PTMT still on baseline #18; Plumbing baseline has changed a third time

Both corrective exports (as-of 2026-08-17, 12 working days remaining) are now **structurally complete** — dual feasibility columns, divergence flags, and cited plan runs are all present and correct. Do not change that work.

But the **baselines are still wrong**, so every figure in both files inherits the error.

---

## 1. BLOCKING — PTMT is still citing plan run #18 / 684,492

```
Original Month Total : 684,492
Baseline Plan Run    : #18
```

It must be **#21 / 617,710**. This has now blocked five rounds.

The bundle was reported built at 12:49 (newer than the 12:16 commit) and the suite is green — so either the publish never happened, or the corrective is resolving to #18 despite #21 existing.

**Report, with evidence:**
- `GET /api/plan/runs?segment=PTMT&month=2026-08` — the full list, with ids, totals, and finalized status.
- Is #21 present and finalized? If yes, why is auto-select returning #18? The route orders `id DESC`, so #21 should win — unless #21 is not finalized, or its month/segment does not match.
- Is the deployed app serving the current bundle? Confirm against the **deployed** URL, not the dev server.

Then re-run the PTMT corrective and confirm `Original Month Total = 617,710`.

---

## 2. Plumbing baseline has now taken a FOURTH value — and moved backwards

| Report | Baseline | Total |
|---|---|--:|
| Earlier capacity-feasible plan | — | 2,330,816 |
| Earlier corrective | — | 2,344,513 |
| Previous session | run **#44** | 2,447,633 |
| **This export** | run **#19** | **2,336,292** |

The run number went **backwards (44 → 19)**, and the total has now taken four different values. The previous prompt asked for this to be reconciled; it has instead drifted further.

**Report:**
- `GET /api/plan/runs?segment=Plumbing&month=2026-08` — all runs with ids, totals, finalized status, created timestamps.
- Why does auto-select return **#19** when **#44** exists and is higher? Was #44 created with the wrong segment casing (`"PLUMBING"` vs `"Plumbing"`) and therefore not matched by the auto-select query? That would tie directly to the casing bug already identified.
- Which run is authoritative for August, and why.

**Do not delete or edit any run** — plan-run immutability is correct. Identify the right one and make auto-select resolve it.

---

## 3. Header vs table totals disagree in both files

| File | Header "Revised Month Total" | Sum of table rows | Gap |
|---|--:|--:|--:|
| PTMT | 721,003 | 721,057 | 54 |
| Plumbing | 2,365,622 | 2,365,721 | 99 |

Small, but a frozen run should agree with itself. Find the cause (rounding at different aggregation points, an excluded category, or a stale header value) and make the header derive from the same summation as the table.

---

## 4. Still outstanding from the previous prompt

- **Segment-casing silent zero** on `POST /plan/runs` — normalise casing or reject unknown segments loudly; never create a zero-item plan run silently. This may be the direct cause of item 2 above.
- **August Plumbing input verification** — confirm every input column is resolved by header name with no silent-zero defaults, listing the exact header text matched and row count per input. The PTMT August plan was inflated by 66,482 pcs from exactly this failure mode and Plumbing's inputs have never been checked.

---

## What is working — do not change

Dual feasibility is now complete and correct in both exports. It shows, with 12 working days left:

| | PTMT | Plumbing | Combined |
|---|--:|--:|--:|
| Remaining | 460,783 | 1,803,499 | **2,264,282** |
| Feasible @capacity | 437,304 | 909,180 | 1,346,484 |
| Feasible @run-rate | 223,092 | 482,266 | **705,358** |
| Shortfall @capacity | 76,012 | 894,319 | 970,331 |
| Shortfall @run-rate | ~237,691 | ~1,321,233 | **~1,558,924** |

**Every category in both segments flags RUN_RATE_DIVERGENCE.** Keep both projections side by side; the batching-vs-constraint question remains a business decision.

---

## Report

- Full plan-run lists for both segments, and why auto-select chose #18 and #19.
- Fresh exports with PTMT on 617,710 and Plumbing on the authoritative August baseline.
- Cause and fix for the header/table total gaps.
- Confirmation the deployed app serves the current bundle.
- Suite count and pass/fail.