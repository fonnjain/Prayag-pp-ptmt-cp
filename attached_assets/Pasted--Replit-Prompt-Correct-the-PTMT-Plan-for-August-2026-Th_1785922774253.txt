# Replit Prompt — Correct the PTMT Plan for August 2026

The engine's PTMT plan for 2026-08 is wrong in **two opposite directions** and must not be released. All inputs come from **uploaded files only — the app must not read any Google Sheet** (existing rule, keep it).

**Current state:** engine total Max = **684,492**. Correct target = **618,010**. Engine is +66,482 (+10.8%) overall, but individual categories are wrong in *both* directions.

---

## FAULT 1 — STOCK IS NOT BEING JOINED (causes over-planning)

For roughly **1,015 code+colour rows**, the engine sets `Stock = 0` even though the uploaded FG file has real stock for that exact code+colour. Since `Production = (Buffer − Stock) + Pending + Pending-LM`, every missed stock value inflates the plan.

**Proof — item 144, Cocks Standard, all colours:**

| | Avg-3-month | Buffer | Stock | → Plan |
|---|--:|--:|--:|--:|
| Engine | 44,380 | 66,570 | **0** | 66,570 |
| Correct | 44,380 | 66,571 | **9,222** | 57,349 |

The over-plan (9,221) is exactly the stock that failed to join.

**Avg-3-month and Buffer are CORRECT — do not change them.** Only the stock lookup is broken.

**Source file:** `F.G Stock on 1st Aug 2026.xlsx` → worksheet **"F.G Sheet PTMT"**, header row 1, three columns only:
- **A = Item Code**
- **B = Colour**
- **C = Closing Stock**

Join on **Item Code + Colour**, normalising both sides identically (uppercase, trim; strip hyphens/spaces/dots on the code; trim + uppercase the colour).

Note this is a **simpler layout than the Plumbing FG file** — there is no "Net Stock" column and **no positive/negative split**. PTMT stock is the plain Closing Stock value. Do **not** apply Plumbing's sign-split logic here.

**Diagnostic lead:** the **Pending-Last-Month** join on the *same* code+colour keys succeeds for 3,476 of 3,636 rows, while the stock join fails on 1,015. Compare the two lookups — whatever differs between them is the defect.

**Sample failures** (code · colour · engine stock · file stock):
`120-C WHITE 0 vs 555` · `120-C IVORY 0 vs 53` · `120-M WHITE 0 vs 3,110` · `120-R WHITE 0 vs 239` · `120-S IVORY 0 vs 142`

---

## FAULT 2 — MISSING ITEMS (causes under-planning)

Four categories come out **below** the correct plan, which a stock-join failure cannot explain — the engine is missing items entirely:

- Cistern & Seat Cover **−10,310**
- Ball Cock **−9,290**
- Faucets & Jetsprays & Shower **−6,317**
- Cabinet **−751**

Also relevant: **304 item codes present in the engine's plan do not exist in the FG upload at all.**

Investigate **both directions** — report which items the engine is missing, and which it has that the source doesn't, per category. Do not silently drop or invent items.

---

## VERIFICATION TARGETS (August 2026)

Report each category PASS/FAIL with the actual value:

| Category | Min | Max |
|---|--:|--:|
| Cocks Standard | 210,513 | 392,794 |
| Cocks Premium | 10,369 | 16,120 |
| Faucets & Jetsprays & Shower | 36,020 | 66,974 |
| Accessorise | 20,011 | 37,506 |
| Cistern & Seat Cover | 22,388 | 38,522 |
| Cabinet | 931 | 2,261 |
| Ball Cock | 34,918 | 63,833 |
| **TOTAL** | **335,150** | **618,010** |

Formula is unchanged and confirmed correct:
`Production Required = (Buffer − Stock) + Pending Order + Pending Order Last Month`, per item, with the category total = **Σ max(per-item, 0)**. Buffer = Avg-3-Month × multiplier.

---

## ADD THESE REGRESSION ASSERTIONS

1. **Stock-join coverage guard** — the count of plan rows with `Stock = 0` where the uploaded FG file has a non-zero value for the same code+colour must be **0**. This is the check that would have caught Fault 1.
2. **Stock reconciliation** — Σ(stock joined onto plan rows) ≈ Σ(FG file stock for codes present in the plan). Report any shortfall in units; a gap of ~498,000 must fail loudly.
3. **Item-coverage guard** — report per category any item in the source that is absent from the plan, and any plan item absent from the source. Both counts must be reported, never suppressed.
4. Apply the equivalent **stock-join coverage guard to Plumbing**, so the same class of silent-zero bug cannot occur there.

---

## Month-rollover note

The July goldens (576,364 / 301,916) are **July** values and must not be asserted against August. Once August verifies correct, capture a fresh August golden set and record the snapshot date.