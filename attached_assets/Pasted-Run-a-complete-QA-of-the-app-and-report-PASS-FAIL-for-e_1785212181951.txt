Run a complete QA of the app and report PASS / FAIL for every point with the actual value the app produces. Fix nothing yet — this is an audit. Run it against the endpoints the UI actually calls, not just the test-only endpoints.

A. SEGMENT ISOLATION

Plumbing shows only its 12 categories, PTMT only its 7 — in every table, sidebar, dropdown, chart and export. No cross-leak.
PTMT totals unchanged: Max ≈ 576,037 (accept the live ~576,364 auto-sync value), Min ≈ 301,918.

B. PLUMBING PLAN (Production Required, per category) — report each vs target (±1%):
CPVC Pipe 130,451 · CPVC Fitting 763,253 · CPVC Solvent 16,539 · UPVC Pipe 51,899 · UPVC Fitting 633,038 · UPVC Solvent 541 · SWR Pipe 64,515 · SWR Fitting 236,315 · SWR Solvent 1,255 · AGRI Pipe 20,299 · AGRI Fitting 54,590 · AGRI Solvent 0. Grand total ≈ 1,972,694 pcs. Item counts: CPVC 40/244/9 · UPVC 52/242/30 · SWR 160/134/3 · AGRI 123/82/1.

C. FORMULA & MULTIPLIERS

ONE formula for all 12 Plumbing categories: max((Buffer − Stock) + Pending + Pending-LM, 0), Stock and Buffer located by header name. Confirm no inverted/special-case formula survives for SWR or AGRI anywhere (plan engine AND corrective engine).
Per-item buffer multiplier read from the sheet; AGRI mapped by header (intentionally differs from source sheet).
PTMT Applied multipliers seeded to business defaults (Cocks Std 1.5, Cocks Prem 1.2, Faucets 1.5, Accessorise 1.5, Cistern 1.2, Cabinet 1.2, Ball Cock 1.5); engine Suggested values are advisory only, never auto-applied.

D. KG FROM BOM

kg = pieces × BOM weight, merging BOTH BOM tabs (Combined + NEW). Per-category vs target (±1%): CPVC Pipe 109,062 · total ≈ 451,421 kg. Guard: CPVC Pipe kg > 1,000 (broken-column detector). No-BOM fraction reported and < 10% (≈5.9%).

E. WEEKLY RELEASE

cover = Stock ÷ Avg-3-Month; bands 0.3/0.5/0.8; each item's full plan in one week; W1+W2+W3+W4 = category total for all 12. Report plant W1–W4 (≈1,859,131 / 12,034 / 82,874 / 18,656).

F. UPLOADS

FG Stock upload is REQUIRED for Plumbing (Col R positives → Stock, negatives → Pending-LM). DATA.xlsx is the global/shared upload. Confirm removing the FG file does NOT silently fall back to sheet numbers — it must error, not produce an empty or wrong plan.

G. CORRECTIVE RE-PLAN (the UI path, POST /corrective/replan — not the test endpoint)

Plumbing producedToDate is read from Sheet3 and is non-zero (≈810,000+ today, NOT 0).
Plumbing capacities are p90 of demonstrated daily output, non-zero (CPVC Fitting ≈34,338, AGRI Pipe ≈790 …), not all-zero.
Code normalisation strips hyphens/spaces/dots (production "A465" matches master "A-465").
Capacity levelling is per category, never a shared pool.
Uses ACTUAL working days remaining (7 as of 23-Jul), not a hardcoded 15.
Report per category: plan · produced · remaining · capacity/day · feasible · shortfall, and confirm unfulfillableQty is a sensible figure, NOT 100% of the plan.

H. PATH PARITY (critical)

Confirm the UI corrective endpoint and the test/validate endpoint return identical results for the same inputs, or that they have been collapsed into one shared engine. A green suite must mean the UI path works. Report whether the parity assertion exists.

I. MONITORING

Two tabs (PTMT/Plumbing), each scoped to its own segment; PTMT reads PTMT ANUJ, Plumbing reads Sheet3, no cross-contamination.
Headline is cumulative released vs cumulative produced (not per-week attainment); run-rate/day shown; percentages suppressed on zero/tiny-release weeks; feasibility banner present.
Unmapped production reported as its own line (≈197,439 pcs); zero-production categories flagged NOT STARTED.

J. OPS DASHBOARD

Segment filter (PTMT/Plumbing/Combined); Plumbing in all aggregate views; KPI cards show real numbers (no "undefined"); Combined keeps pieces and kg separate.

K. EXPORTS

Production Plan (Excel+PDF) and Corrective Plan present, side by side. Corrective has both a standard-format Excel (schema-identical to the main plan export — assert headers match cell-by-cell) and a full-detail Excel. Plumbing workbooks have 12 category sheets, PTMT 7. kg present. Every export states its plan-run ID / as-of date.

L. IMMUTABLE PLAN RUNS (if implemented)

Re-running creates a new version; prior versions unchanged. "As issued" vs "if re-run today" both visible with their difference. Corrective run cites the baseline version it measured against.

M. TEST SUITE

Report total check count and how many pass. Confirm the suite exercises the UI paths, not only internal ones. List any assertion still on a ±5% tolerance (should be ±0.1%).

Finally: give a one-line PASS/FAIL summary per section A–M, and list the top issues found in priority order. Be honest about anything not implemented or not matching — do not report green unless the actual value matches.