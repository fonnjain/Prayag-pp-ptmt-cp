The corrective exports are close. Baselines are correct (PTMT #20 / 617,711, Plumbing #21 / 2,331,647) and all the core arithmetic reconciles exactly — feasible = cap × 12, remaining = plan − produced, shortfall = max(remaining − feasible, 0), and item sheets sum to the Summary Min and Max in both Standard files, 19/19 categories, no tolerance. Five things left:

Header vs table, Original side. buildCorrectiveDetailExcel:548 still uses Math.round(run.originalMonthTotal) (stored 32-bit real) while :549 correctly uses grandPlanComputed. PTMT header 617,711 vs Standard Min TOTAL 617,750 (+39); Plumbing 2,331,647 vs 2,331,750 (+103). Add grandOrigComputed = items.reduce((s,i) => s + Math.round(i.originalPlan), 0) and use it at :548 and in the baseline citation at :552. Then assert header == table for both Min and Max, in both Standard and Detail. Separately: PTMT now reports 617,711 where the verified golden is 617,710 — reconcile the extra unit, don't paper over it.
Capacity/Day, Feasible and Shortfall are category values stamped on every item row. Summing the Shortfall column gives 85,905,324 for CPVC Fitting against a true 352,071, and item S5 shows plan 0 / remaining 0 / shortfall 24,338. Either compute per item or move all three out of the item grid into a per-category block. Priority for the Standard export — a downstream consumer aggregating that column is off by ~240×.
NO_DEMONSTRATED_CAPACITY, NOT_STARTED and ZERO_CAP_WITH_PRODUCTION appear nowhere in either workbook. The four Plumbing Solvent categories have cap 0 / produced 0 / plan > 0 and show only UNFULFILLABLE_THIS_MONTH. The engine computes these at corrective-engine.ts:804–805; render them.
Plumbing exports 1,123 items, not 1,120. The three AGRI sheets each have an extra row whose Item Code cell holds the AGRI explanatory note. Move it to the Legend or below a blank separator, and add a regression check asserting 1,120 items with the per-category split (CPVC 40/244/9 · UPVC 52/242/30 · SWR 160/134/3 · AGRI 123/82/1).
Two shortfall numbers disagree by ~2.5%. Items flagged UNFULFILLABLE sum to 911,571 remaining (Plumbing) against a Summary shortfall of 889,675; PTMT 20,476 vs 15,541. Document the relationship in the Legend or add a reconciliation row — right now the file gives two shortfall figures and no guidance on which to quote.