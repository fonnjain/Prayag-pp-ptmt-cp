# PROMPT 4 — Finish the Sunday sweep + outstanding verification

`61d9c14` is verified and correct. Both the calendar-as-base rule (`plant-lifecycle.ts:99–100`) and the open-month projection (`:103–108`) are implemented exactly, and `monitoring.ts:302` / `plan.ts:2223` are covered.

**Three Sunday-exclusion sites remain, and four verification items from the previous round were not reported.** Do the read-only part first; do not change code until Part A is reported.

**Record correction:** the prompt-3 SHA is **`61d9c14`**, not `d0546b1`. That SHA does not exist in the repository and has been cited three times. Please fix it in the task log.

---

# PART A — READ-ONLY. Report before changing anything.

## A1. Does 31 May 2026 have production? ⚠ time-sensitive

`plant-lifecycle.ts:36–44`:

```typescript
export function lastWorkingDay(month: string): string {
  …
  if (date.getUTCDay() !== 0) return `${month}-${String(day).padStart(2, "0")}`;
```

This sets the snapshot date for a closed month, and it skips Sunday. If a month **ends on a worked Sunday**, the snapshot lands a day early and that day's production is excluded from the frozen month — **permanently**, because the snapshot is immutable by design.

In FY26-27 that is **May 2026 (already closed), January 2027, February 2027.**

For both segments, report for **2026-05**:

- total loaded rows and quantity
- rows and quantity on **31 May 2026** specifically — state zero explicitly if none
- what `lastWorkingDay("2026-05")` returns today

If 31 May has production, May's figures are already short and the fix must land before May is ever captured. **Do not capture a May snapshot** either way until this is fixed.

## A2. Corrective feasibility impact — quantify before fixing

`corrective-engine.ts:226` counts `workingDaysRemaining` excluding Sundays, while `dailyByCat` (`:470–478`) includes them with no date filter — so `computeCapByCategory` (`:527–533`) derives Cap/Day from all observed days. `feasible = capPerDay × workingDaysRemaining` therefore multiplies a Sunday-inclusive capacity by a Sunday-excluded remainder.

Feasible is understated and shortfall overstated. The numerator is fine — `producedToDate` sums all rows undated.

For **PTMT August** and **Plumbing August**, per category, report:

| Cap/Day | days used in the p90/mean (`vals.length`) | workingDaysRemaining (current) | workingDaysRemaining incl. worked Sundays | feasible now | feasible corrected | shortfall delta |

Plus the segment totals. I want the size of the error before deciding how to fix it.

## A3. Linearity, before and after — still owed

Per segment for June, three values so the two corrections are separable:

1. before `d0a7c9a` (numerator wrong, denominator wrong)
2. after `d0a7c9a` (numerator fixed)
3. after `61d9c14` (both fixed)

Linearity was depressed from both directions, so it moves most. **Do not adjust the 0.60 back-loading threshold** — report the numbers; recalibration is a separate decision once we can see them.

## A4. Open-month ordering check — still owed

The §4 hazard: `elapsed` was computed before `dailyByDate` existed. A **closed** month takes the `elapsed = workingDays` path and would look correct even if the reorder was missed, so June proves nothing.

For **PTMT August** (open), report `elapsed`, `elapsedDays.length`, and `workingDays`. Expect **18 / 18 / 28** at the Aug 19 snapshot.

## A5. Deployed commit — still owed

`GET /healthz` after `46c36ae`. If `commitSha` is still `(unknown)`, the publish did not build from `main` — that has happened before. Report `commitSha` and `dbHostname`.

---

# PART B — Code changes, after Part A is reported

Two commits, separately reviewable.

## Commit B1 — `lastWorkingDay` must honour worked Sundays

Take the last day of the month that is **either** a calendar non-Sunday **or** a day with production:

```typescript
export function lastProductionDay(month: string, observedProductionDates: string[] = []): string {
  const worked = new Set(observedProductionDates.filter(d => d.startsWith(month)));
  …
  for (let day = lastDay; day >= 1; day--) {
    const iso = `${month}-${String(day).padStart(2, "0")}`;
    if (new Date(`${iso}T00:00:00Z`).getUTCDay() !== 0 || worked.has(iso)) return iso;
  }
  return `${month}-01`;
}
```

Same shape as the `61d9c14` rule — calendar base, union worked days. Update every caller to pass the observed dates.

**Test the exact case:** a month ending on a worked Sunday returns that Sunday, not the Saturday. Use 2026-05 as the fixture if A1 shows production on the 31st; otherwise construct it synthetically.

## Commit B2 — corrective feasibility basis

Make `workingDaysRemaining` consistent with the basis Cap/Day was derived from. Since `dailyByCat` includes Sundays, remaining must too: calendar non-Sundays after the as-of date, **plus** any Sunday already worked in that window, projected the same way `plant-lifecycle.ts:103–108` does it.

Do not change `computeCapByCategory` — Cap/Day is already correct.

While in this file, align `machine-capacity-engine.ts:70`: it uses local **`getDay()`** where everything else uses **`getUTCDay()`**, so on an IST server the two disagree around midnight boundaries. Same worked-Sunday treatment, same UTC basis.

**Expect shortfall to fall.** Report the before/after totals per segment for August — that number goes to the team, since shortfall drives the corrective plan.

---

## Report back

Part A first, as data only. Then for each commit: files and line numbers, diffstat with **every file named including `package.json`**, `tsc --noEmit` clean, test count, byte-size match, and confirmation that nothing else is in the diff.

Keep the live Plumbing regression checks reported separately from the API unit suite, as you have been — the Sheets 503/403 blockage is unrelated and shouldn't be folded into a pass count.