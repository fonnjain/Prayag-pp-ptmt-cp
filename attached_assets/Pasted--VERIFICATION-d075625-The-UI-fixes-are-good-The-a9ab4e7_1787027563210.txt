# VERIFICATION — `d075625`

The UI fixes are good. **The `a9ab4e7` check regression is still open and was not mentioned in this round.**

---

## ✅ What landed correctly

- **Velocity** now destructures `isError` and `error` (`:12`) and returns a dedicated error branch at `:45–72`, splitting 503 ("not configured", with the deploy-secrets note) from everything else ("API returned: HTTP 502"). It prints both diagnostic URLs and offers Retry. The footer scope note — *"PIPE plant only (fitting/solvent excluded)"* — makes the hardcoded `plant: "PIPE"` visible for the first time.
- **Recommendations, Trend, Config** each gained `setError` and a rendered error branch. No silent catches remain in any of them.
- **Reports** now throws on `!res.ok`, stores `loadError`, and renders it inside the runs card.

Diffstat is +108/−18 across exactly the five files described, no truncation.

## Your two diagnostic findings are consistent with the code

**502, not 503** rules out the missing key and points at `prayag-plant.com` itself — either the 20 s `UPSTREAM_TIMEOUT_MS` at `plant-live.ts:8` or a non-2xx from upstream. The proxy collapses both into 502 (`:97`), so the two are still indistinguishable from the client. The upstream status is logged server-side (`logger.warn({ status, qs })` at `:96`) — that log line will separate them. `GET /api/plant-live/periods` remains the fastest way to establish whether upstream holds `2026-08` at all.

**Dashboard 200 at 24 s first hit** is worth noting separately: that is close enough to timeout territory that it's a latency problem in its own right, not just a caching artefact. Worth a look once the pages are visible.

---

## ❌ Still open: the ExportTotals regression from `a9ab4e7`

This commit touched only the five frontend pages. In `corrective.ts` at HEAD:

```
:966  let detailOrigHeader = items.reduce((s, i) => s + Math.round(Number(i.originalPlan ?? 0)), 0);
:967  let detailPlanHeader = items.reduce((s, i) => s + Math.round(Number(i.planRev ?? 0)), 0);
:985  let stdGrandMin   = items.reduce((s, i) => s + Math.round(Number(i.originalPlan ?? 0)), 0);
:986  let stdGrandMax   = detailPlanHeader;
```

ExportTotals check count is **6** — still down from 8.

So the position is unchanged from last round: six checks pass by comparing a precomputed value to itself whenever the workbook parse finds nothing, and the two `≤ 100 pcs` divergence checks remain deleted. These are the checks that caught the original 39/103 header-vs-table bug.

Restoring the `-1` sentinels is a four-line change. Worth doing before the publish, since publishing is what makes the export path live in production.

## Minor: two silent catches remain

`plumbing-reports.tsx:58` and `:74` — the Excel and PDF download handlers:

```typescript
if (!res.ok) throw new Error(`${res.status}`);
…
} catch { /* silent */ }
finally { setExporting(null); }
```

A failed export leaves the spinner stopping with **no file and no message** — the user sees a button that did nothing. Given the corrective PDF path is the one that was hanging on `protocolTimeout` a few rounds ago, this is the exact interaction where a silent failure is most likely and least explicable. Same `loadError` pattern you just added above would cover it.

---

## Suggested reply

> UI fixes verified — Velocity has a real `isError` branch splitting 503 from 502 with both diagnostic URLs and Retry, the "PIPE plant only" footer makes the hardcoded scope visible, and Recommendations / Trend / Config all have `setError` plus a rendered branch. Reports throws on `!res.ok` and shows `loadError` in the runs card. Diffstat +108/−18 across the five files, no truncation.
>
> Your diagnostics line up with the code. **502 rules out the missing key** and points at `prayag-plant.com` — but the proxy collapses "upstream non-2xx" and "20 s timeout" into the same 502 at `plant-live.ts:97`, so those are still indistinguishable client-side. The server log at `:96` (`logger.warn({ status, qs })`) carries the real upstream status — that's the line to read. `GET /api/plant-live/periods` will settle whether upstream holds `2026-08` at all. Separately, **the dashboard's 24 s first hit** is close enough to timeout territory to be worth treating as its own issue once the pages are visible.
>
> **Two things still outstanding:**
>
> 1. **The `a9ab4e7` ExportTotals regression is unfixed** — not mentioned this round. At HEAD, `corrective.ts:966/967/985/986` still initialise `detailOrigHeader`, `detailPlanHeader`, `stdGrandMin` and `stdGrandMax` to the same reductions they're compared against, and the check count is still 6 (was 8). Six checks pass by comparing a value to itself whenever the workbook parse finds nothing, and the two `≤ 100 pcs` divergence checks are still deleted. These caught the original 39/103 bug. Restoring the `-1` sentinels is four lines — please do it **before** the publish, since publishing is what puts the export path into production.
>
> 2. **`plumbing-reports.tsx:58` and `:74` still have `catch { /* silent */ }`** — the Excel and PDF download handlers. A failed export stops the spinner with no file and no message. Given the corrective PDF path was hanging on `protocolTimeout` a few rounds ago, that's the interaction most likely to fail silently. Same `loadError` pattern as above.