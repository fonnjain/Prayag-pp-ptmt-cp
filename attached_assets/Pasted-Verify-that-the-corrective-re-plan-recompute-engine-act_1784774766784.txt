Verify that the corrective re-plan (recompute) engine actually implements the logic below. For each numbered point, report PASS / FAIL / NOT IMPLEMENTED with the actual behaviour you find in the code, and quote the relevant values. Do not change anything yet — this is an audit. Where something fails, say exactly what it does instead.

1) TRIGGERS. Confirm the re-plan can be run: (a) at a week boundary via the "Week just closed" selector (None/W1/W2/W3), (b) on demand via "Re-plan now", and (c) as of an arbitrary date defaulting to today. Confirm running "as of the last day of W1" gives an identical result to selecting "Week just closed = W1".

2) LIVE RECOMPUTE OF THE REQUIREMENT. Confirm the re-plan recalculates each item's requirement from current live data (stock, pending orders, new orders) rather than reusing the original month-start plan. Confirm the formula is max((Buffer − Stock) + Pending Order + Pending Order Last Month, 0) per item, with Buffer = Avg-3-Month × the per-item multiplier read from the sheet by header name. State where each input is read from.

3) PRODUCTION SUBTRACTION. Confirm remaining_to_produce = max(plan_revised − produced_to_date, 0) is computed per item and then summed per category — i.e. overproduction on one item must NOT offset a shortfall on another. Confirm production is read from PTMT ANUJ for PTMT and Sheet3 for Plumbing, with no cross-contamination between segments.

4) CODE NORMALISATION. Confirm production codes are matched to plan items on a normalised code (uppercase, strip hyphens, spaces, dots) — e.g. master "A-465" must match production "A465". Report how many production pieces currently fail to match any plan item, and confirm that figure is reported as UNMAPPED rather than silently dropped.

5) CAPACITY AND FEASIBILITY. Confirm: capacity_per_day = p90 of that category's demonstrated daily output (not a theoretical or circular figure derived from the plan itself); working_days_remaining = days from the as-of date to month end excluding Sundays; feasible = capacity_per_day × working_days_remaining; shortfall = max(remaining − feasible, 0) raising UNFULFILLABLE_THIS_MONTH. Report the current per-category capacity_per_day values and confirm none is derived from the plan quantity.

6) PER-CATEGORY LEVELLING. Confirm capacity is applied per category and never as a shared plant pool — a category with spare capacity must not absorb another category's overload. Confirm no category is ever scheduled above its own capacity in any week.

7) RE-TIMING AND CARRYOVER. Confirm remaining work is re-assigned across the remaining weeks using cover = Stock ÷ Avg-3-Month with bands 0.3 / 0.5 / 0.8; that carryover from earlier weeks is re-prioritised and never dropped; and that urgency is re-scored on every run (items at zero cover that have never been produced stay top priority).

8) VARIANCE ATTRIBUTION AND WARNINGS. Confirm the change from the original plan is split into production shortfall vs new demand vs capacity infeasibility, and that these warnings exist and fire: WEEK_LAG, CAPACITY_OVERLOAD, UNFULFILLABLE_THIS_MONTH, NEW_DEMAND_SPIKE, STOCKOUT_IMMINENT.

9) DUAL UNIT. Confirm the corrective output reports both pieces and kilograms for Plumbing, with kg computed as pieces × BOM weight-per-piece (never copied from the source sheet's kg column).

10) PLAN INTEGRITY. Confirm the re-plan does not alter total plan quantities except where genuinely driven by new demand — it re-times when work is released and reports what is unachievable; it must not silently reduce a category's requirement to make it fit capacity.

11) SEGMENT SCOPE. Confirm the re-plan runs independently for PTMT (7 categories) and Plumbing (12 categories), with no shared state, and that PTMT figures are unaffected by Plumbing runs and vice versa.

Finally, run the re-plan for Plumbing as of today and report: per category — plan, produced, remaining, capacity/day, feasible, shortfall — plus the totals and any UNFULFILLABLE quantities. For reference, an independent calculation as of 14-Jul-2026 (12 working days used, 15 remaining) gave: total plan 1,972,695 · produced 626,470 · remaining 1,346,226 · feasible 1,065,255 · shortfall ≈ 280,971, with UPVC Fitting the worst at 462,822 remaining vs 252,150 feasible. Today's figures will differ since more days have elapsed — report the actuals and flag anything that looks inconsistent with that trajectory