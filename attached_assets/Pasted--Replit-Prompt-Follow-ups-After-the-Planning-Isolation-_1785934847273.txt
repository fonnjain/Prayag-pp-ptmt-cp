# Replit Prompt — Follow-ups After the Planning-Isolation Work

The isolation scope is accepted as built (347/347). Both flagged decisions were correct — moving the Live Order column outside the planning context, and sourcing Plumbing current pending from DATA.xlsx balance columns rather than the FG Stock file (my original spec was wrong on that; the goldens re-roll to 1,950,983 is the right handling).

Five follow-ups, in priority order.

---

## 1. PUBLISH the August fix (blocking — do this first)

The corrected August PTMT plan (Max 617,710) only reaches users once deployed. Publish, then confirm the deployed app returns the corrected figures — not the previous 684,492.

This has bitten us before: two consecutive Plumbing exports came back byte-identical because a change was committed but never deployed. Verify against the running app, not the working copy.

---

## 2. Tighten the sales sanity band

The current band is **0.4–2.5×** vs prior month. That catches a collapse to zero, but a 40% sales drop passes silently — and since avg-3-month drives Buffer, that cuts the plan by the same proportion across every category. Sales is the one planning input deliberately left live and unlocked, so it deserves a tighter net.

Change to:
- **Hard fail** outside roughly 0.6–1.6×
- **Advisory warning** (surfaced in validate output and the UI, plan still builds) outside roughly 0.85–1.2×

The current ratio is ~1.0, so tightening costs nothing today.

---

## 3. Confirm the computed-not-copied defence ordering

Two mechanisms exist: (a) stock/pending/buffer columns are **no longer mapped at all**, and (b) a **pattern tripwire** fails on headers matching Production-Required / Min / Max.

Confirm (a) is the primary defence and (b) is the backstop — not the reverse. A pattern match is a name check, so a differently-labelled computed column would slip past it; not mapping the columns at all is the guarantee that holds regardless of naming. State plainly which one is load-bearing.

---

## 4. Explain the Accessorise residual (+2.74%)

Six categories land within ~1.7% of target; **Accessorise is +2.74% — the only one OVER**. Missing roster items can only *reduce* a total, so roster coverage does not explain this one. Something else is inflating Accessorise.

Investigate and report the cause at item level. Do not assume it shares the same explanation as the other residuals.

For reference, current vs target (Max): Cocks Standard 392,223 / 392,794 · Cocks Premium 15,875 / 16,120 · Faucets 66,886 / 66,974 · **Accessorise 38,533 / 37,506** · Cistern 37,861 / 38,522 · Cabinet 2,258 / 2,261 · Ball Cock 64,075 / 63,833.

---

## 5. Item-master coverage report (data-governance input)

**353 source codes hold stock (137,558 units) but are absent from the item master**, and **305 plan codes have no FG entry**. Until closed, the plan under-produces those items — the mirror of the bug just fixed.

Produce a clean report, per category, listing both directions with quantities, so the business can decide which are genuinely planned products (e.g. DB-02L, PH-01/02, the PTA family) and which are dead codes. **Do not add or drop items unilaterally** — this is a business decision, not an engineering one.

---

## 6. Complete the immutable plan-run work (before month-end)

Because sales stay live, the same month's plan legitimately produces different numbers on different days. On 28 July that drift cost a full diagnostic cycle to prove benign, and it will recur every month under this scope.

Finish the immutable plan-run feature so the issued plan is frozen as a baseline: "as issued" vs "if re-run today" both visible with their difference, and every corrective run citing the plan-run version it measured against. That converts drift from an alarm into an explanation.

---

## Report

PASS/FAIL per item above, the Accessorise root cause at item level, the coverage report, and the new suite count.