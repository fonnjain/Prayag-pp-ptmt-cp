Structure is fully correct in both segments — header vs table reconciles to 0 on Original and Revised, item counts are exactly 3,636 and 1,120 in Detail and Standard, Cap/Day is None on every item row, and the three Plumbing Solvent KPI rows carry NOT_STARTED | NO_DEMONSTRATED_CAPACITY. All 19 categories pass the four invariants with no tolerance.

1. The PTMT citation gives us the first real read of the frozen run, and it's 617,710 — not 617,711. frozenPlanGrandMax = Σ productionPlan for plan run #20 = 617,710. So LOGIC_PTMT §A7 needs correcting again: 617,710 is production plan run #20's item sum, not a "dev-sequence golden". The three measured values are: 617,710 (plan_run_results), 617,711 (corrective_plan_runs.original_month_total), 617,750 (corrective_plan_items, Σ Math.round). The 40-pcs gap is inside ±200 so no MISMATCH — correct. NC20b is unaffected.

2. Plumbing fell to the legacy path — frozenPlanGrandMax is NULL on that corrective run. It isn't an empty plan run: the aggregate uses coalesce(sum(...), 0), so zero result rows would persist 0 and print "plan run: 0 pcs" with a MISMATCH, not "not recorded". The run simply predates migration 022 / the hoist and is being re-exported rather than re-run. Please re-run the Plumbing corrective and confirm the citation shows a frozen total — that also gives the first actual measurement of #21's frozen total, which is still only known from a commit message (2,331,647), never read back.

While you're there: add the corrective run's own id and created-at to the Excel Summary. The PDF has Run #{run.id}, the Excel doesn't — nothing in the workbook distinguishes a fresh run from an old one being re-exported, which is precisely what caused this.

3. Heads-up on the capacity ladder: with more actuals ingested, demonstrated capacity fell for Accessorise (3,609→2,476), Cistern (1,490→1,240) and Ball Cock (6,048→5,289) while production rose 47,000 — the new days sat below the previous p90. Total feasible dropped 437,304 → 413,424. Working as designed, but feasibility worsening on a good production day will read as a bug to anyone scanning the Summary. Rendering days observed and method (p90/mean) on the KPI row would make it legible; the engine already carries capacityMethod and capacityDays.