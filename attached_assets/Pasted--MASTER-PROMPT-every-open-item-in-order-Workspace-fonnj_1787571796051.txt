# MASTER PROMPT — every open item, in order

**Workspace: `fonnjain/Prayag-pp-ptmt-cp`.** Several recent prompts landed in Distributor Analysis by mistake — confirm before starting.

**Current state:** `origin/main` = `8181903`. Production = `8c2e7f5`, **four commits behind**. Nothing may be published yet (see §1).

Nine open items. They are ordered so that each one's prerequisite is already done when you reach it. **Do not reorder.**

---

# §1 — #157: the July golden question ⬤ BLOCKS THE PUBLISH

Nothing can be deployed until this resolves. `0a3fe7d` is in the chain and carries an unexplained change.

`0a3fe7d` lowered the July UPVC Solvent golden from **541 → 525**, with the reason "re-read from the live July workbook". July is a **closed** month — its figures are meant to be frozen, so a drifting July golden is itself the finding.

Three possibilities, and only one makes the change correct:

1. Someone edited the July source workbook after close
2. The golden was always wrong and 525 is true
3. **The parser changed** — `8b2c381` modified `sheets.ts` for legacy Plumbing layouts, and the very next commit moved a Plumbing golden downward

**Report, in this order:**

- The July Plumbing workbook's **`modifiedTime`**. July closed 8 August. An edit after that is the finding.
- Re-run the **pre-`8b2c381` parser** against the **current** workbook. If it still yields **541**, the parser changed and the golden must be **reverted**, not recorded.
- **What the sheet actually contains** for July UPVC Solvent. Read the cell directly — that is the arbiter, not either parser.

If case 3, revert `0a3fe7d` and fix the parser instead.

---

# §2 — #167: regression verifier authentication

Environment, not code. It blocks §3 and it has now blocked three consecutive verification attempts.

All three active admins return **401** before any check executes. The verifier needs a valid existing admin via `REGRESSION_AUTH_EMAIL` / `REGRESSION_AUTH_PASSWORD`.

**When it runs, expect failures that predate this work.** The last full run was **4/98**, and §1 is unresolved. **Report a named list, not a count** — a red result must not be read as a regression from the recent commits.

---

# §3 — Exercise item 4's assertion against real data

Item 4 added the check that a populated source with zero recognised rows fails (`verify-plumbing-plan.ts:207`). **It has never met a live condition** — every run since has stopped at 401.

Once §2 is fixed, run it and report what the diagnostics say for each source: rows read, rows recognised, resolved and accepted aliases, and present headers.

**Expect the pending-order source to fail this check**, since it currently recognises 0 of 123 PTMT rows. That failure is correct and is the proof the assertion works.

---

# §4 — #166: preserve the order snapshot per plan run ⬤ do before the 0b decision

**The only item that gets worse with time.** Every plan run created without this is another month that cannot be audited later.

The gap: run #20's `pending_current` is 0, and **there is no 17-August snapshot of the open-order book**. The plan froze its items, totals and provenance — but not its order inputs. So *"what were the outstanding orders when #20 was created?"* has no answer from stored data.

This workstream has hit the same shape three times: the June workbook drift, the July golden, and now this. Each was "capture it later" until later arrived.

**Design:** `frozenPlanGrandMax` already captures the plan total at run time for exactly this reason. Apply the same pattern to the inputs — store the pending-order rows, and their diagnostics, alongside the plan run, keyed to it, immutable.

**Why before 0b:** whichever way the business answers, the answer only helps future runs if their inputs are preserved. Landing this first means the next plan run is auditable regardless of the decision.

---

# §5 — 0b: the pending-order business decision ⬤ needs the team, not code

`calc.ts`:

```
Max Production = (Buffer Req − Stock) + Pending Order + Pending Order Last Month
```

**Does `Pending Order` mean open balance, or invoiced quantity?**

- **Open balance** → `DATA.xlsx` is the wrong source, and plan runs **#20 and #21 understate every item with outstanding orders on 17 August**. A plan defect the corrective merely inherits.
- **Invoiced quantity** → the current zero is wrong for a different reason, and the live sheet is the wrong source instead.

One of the two paths is reading the wrong thing today.

**State plainly to the team:** run #20 **cannot be corrected retrospectively** — its inputs were never preserved. Only future runs can be right. That changes what "fix it" can mean, and they should know it before deciding.

Supporting figures to report with the question:

- The live sheet holds **15,238 pcs across 71 PTMT keys** today
- The upload holds **3,766 PTMT rows** with invoice `Quantity` and **no** open-balance column
- 71 keys vs 3,766 rows is consistent with a balance register vs a transaction log — confirm it
- Is an open-balance export available at all, or does the business only produce the invoice register?

---

# §6 — Item 1: the pending-order header fix ⬤ blocked on §5

Only after §5 is answered.

`sheets.ts:1083–1105` — `rowsToObjects` (`:663–673`) matches header names **verbatim**:

| Parser expects | Sheet has | Result |
|---|---|---|
| `Segment` | `SEGMENT` | every row skipped at the filter |
| `Old ERP Code` | `Item Code` | skipped by `if (!code)` |
| `Colour` | `COLOR` / `COLUOR` | empty |
| `Bal. Qty` | `Bal. Qty` | ✅ only match |

**15,238 pcs invisible.** This is HANDOVER §7 lesson 1 verbatim, in production, on the order input.

**Fix:** normalised matching (trim, case-fold, collapse punctuation) against alias lists. **`COLUOR` is a typo in the source sheet — match it as an alias**, do not ask anyone to correct the sheet.

**Same commit:** `fetchLivePendingOrderTotals()` hardcodes `if (segment !== "PTMT") continue` with no segment parameter, so **Plumbing pending orders can never be returned** — zero by construction, not by data. Add the parameter and use `normalizePlantSegment`.

**Do not add `Quantity` to the plan's alias list** as part of this. `plan.ts:585` already accepts `"Qty"`; the upload's field is `Quantity`, so the plan's zero is one letter, not a design decision. Had it matched, the plan would have taken **827,733** pcs of invoice quantity as pending. That belongs to §5.

---

# §7 — #165: the last swallowing catch ⬤ same commit as §6

`corrective-engine.ts:461` still swallows `fetchLivePendingOrderTotals()` failures.

Items 2 and 3 removed every other silent path in that block — **this is the only one left.** And the moment §6 lands, this catch turns a fetch failure into `deltaNewOrders = 0 − pendingAtPlan`, silently reverting the order component to exactly what §6 fixes.

**Ship it with §6**, not before and not later.

---

# §8 — #148: recapture the July PTMT snapshot

Gate already passed — production resolves July PTMT as `workingDays 28 / observed / elapsed 28`.

The row was captured **2026-08-19**, the day before the Sunday fix (`d0a7c9a`), so its KPIs are pre-fix: `producedToDate` **898,817**, short by the **9,029** pcs made on Sunday 5 July, and `workingDays` **25** where July observed 28. Migration 028 left `captured_commit_sha` NULL, so nothing on the row records this.

**Steps:**

1. **Archive first — not optional.** Export the current row, SHA-256 it, commit under `.agents/outputs/` in the `047448e` pattern. It is the only record of what the pre-fix code produced.
2. Delete and recapture that single row.
3. **Reconcile:** the recaptured `producedToDate` should be about **907,846** — the old 898,817 plus the 9,029 Sunday, with the 86,528 unmapped still excluded. Report the actual figure and explain any difference.
4. Set `captured_commit_sha` and rewrite `plan_status_reason` to record the recapture and the original 2026-08-19 date.

**Do only after §1 clears and the publish has happened**, so the deployed code matches what recaptures the row.

---

# §9 — Publish

Only after §1 resolves.

Then confirm `/healthz commitSha` equals `git rev-parse origin/main`, and re-run the verifier reporting the named list.

---

## Reporting

Each item its own commit where code changes. Files and line numbers, diffstat with **every file named including `package.json`**, `tsc --noEmit` clean, test count, byte-size match, nothing else in the diff.

Report the live verifier separately from the API unit suite, as you have been.

## Two standing notes

**The 401 is now a three-time blocker.** Three consecutive verifications have been validated by unit tests alone. That is not the same as verified, and §2 exists to end it — please treat it as work rather than a documented limitation.

**A task is open until its work is done, not until its note is written.** Nine tasks have been archived or cancelled in this workstream with the underlying work outstanding; two of those left live defects in production. If any item above cannot be completed, report it open rather than closing it with the constraint recorded.