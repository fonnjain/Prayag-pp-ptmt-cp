# Replit Prompt — Deploy the fixes, then restore the Plumbing August baseline

Two blocking items. Do them in order. **Item 1 first — until it is done, none of the work verified over the last several rounds exists for users.**

---

## 1. BUILD AND REDEPLOY (blocking)

The production CJS bundle (`dist/index.cjs`) was never rebuilt after the baseline-fix commit (`5fbd5e1`). The live server is still running the **pre-fix bundle**. The dev server on port 8080 returns the correct values; production does not.

This also explains the three byte-identical corrective exports — they were served by a production build that predates every fix.

**Do:**
- `pnpm build` in the `api-server` package, then redeploy.
- **Verify against the DEPLOYED app, not the working copy:**
  - PTMT August Max = **617,710** (not 684,492)
  - The corrective cites **baseline plan run #21**
  - `dailyCapacity` is the p90 figure (31,826/day), not the removed `21335` fallback

Report the deployed values explicitly. A dev-server check does not close this item.

**Then add a guard so this cannot recur:** a check (CI step or startup assertion) that fails when the built bundle is older than the latest commit touching the api-server package. Three rounds of review were spent on numbers that were never live.

---

## 2. RESTORE THE PLUMBING AUGUST BASELINE

The Plumbing corrective currently returns **all zeros** — `baselinePlanRunId: null`, `baselineSource: live`. No finalized Plumbing plan run exists for August, so the engine falls back to a live rebuild, and with no August upload data seeded it has nothing to compute from. Plumbing corrective and dual feasibility are non-functional until this is fixed.

**Do:**
- Confirm which Plumbing August uploads are present and which are missing (FG Stock file, DATA.xlsx). Report the row counts and upload dates.
- Seed the missing August uploads.
- Build the August Plumbing plan and **finalize a plan run**, so `baselinePlanRunId` resolves and the corrective stops falling back to a live rebuild.
- Re-run the Plumbing corrective and report per category: plan · produced · remaining · cap/day · method · days observed · feasible@capacity · feasible@run-rate · daysRun/daysElapsed · divergence flag · shortfall.

**Note:** the "no finalized run → silent live rebuild → all zeros" path is itself a hazard. A missing baseline should surface as a **named error**, not as a plan of zeros that looks like a legitimate result. Add that guard: when `baselinePlanRunId` is null AND the live rebuild yields zero items, fail loudly rather than returning an empty corrective.

---

## 3. CONTEXT — do not change the feasibility method yet

The PTMT read-out shows **all seven categories** diverging at 0.40–0.64× of the modelled rate — plant-wide, not selective. Capacity projection says 625,872 feasible (shortfall 12,331); run-rate projection says 346,030 feasible (**shortfall ~290,000**). The W1 warning corroborates the pessimistic view: produced 160,025 against 489,818 released, a 67% shortfall.

Notably **Cocks Standard ran 9 of 9 days and is still only 0.64×** — so its gap cannot be explained by campaign batching; it ran every available day and still underperformed the model.

This is a business question (planned batching vs genuine constraint), not an engineering one. **Keep reporting both projections side by side and leave the method unchanged** until the plant answers it. Do not silently switch to either figure.

---

## Report

- Deployed-app values for PTMT August Max, cited plan run, and dailyCapacity.
- Plumbing August upload status, the finalized plan run id, and the full per-category corrective read-out with dual feasibility.
- Confirmation of the two new guards (stale-bundle check, empty-baseline loud failure).
- Suite count and pass/fail.