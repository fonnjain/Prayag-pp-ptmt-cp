# VERIFICATION — `2b36b16`

All three items landed. **But check 7 will fail on Plumbing the moment it runs, and the comment justifying the threshold has its own numbers swapped.**

---

## ✅ Check count is 8

Both divergence checks restored at `:1167` and `:1173`, asserted via `checks.push`, with `tolerance` strings citing §A2. The two-axis comment (1–6 equality, 7–8 divergence) is accurate and worth keeping.

## ✅ PATCH is properly gated

Order is correct — `requireApiKey` as middleware, then 400 validation, then **403 on `NODE_ENV === "production"`** (`:317`), then 404, then **409 on `pinned`** (`:340`), with `note` updates still permitted on pinned runs. Structured logging carries `frozenOld`/`frozenNew` and `noteOld`/`noteNew` plus `pinned`. That is a stronger design than I suggested — the production block makes the field genuinely untouchable where it matters, rather than merely authenticated.

Two follow-ups on this:

- **`requireApiKey` is now duplicated** — `corrective.ts:14` and `plant-live.ts:18` are separate copies of the same function. Two copies of an auth guard drift; one gets a fix the other doesn't. Worth hoisting into shared middleware.
- **Confirm `NODE_ENV=production` is actually set on the Replit deployment.** Nothing in `package.json`, `.replit` or the start script sets it, and `logger.ts:3` is the only other consumer. If the deploy runs without it, the 403 never fires and the only remaining protection is the Bearer token. Worth checking `/healthz` or the deploy env directly — a guard keyed on an unset variable is exactly the silent-default failure mode from §7.

---

## ❌ Check 7 fails on Plumbing with current data

The threshold is `≤ 100 pcs`. The measured Plumbing divergence, from the exports you sent two rounds ago:

| Segment | Items | `detailOrigHeader` | `storedOrig` | Divergence | vs ≤100 |
|---|--:|--:|--:|--:|---|
| PTMT | 3,636 | 617,750 | 617,711 | **39** | pass |
| **Plumbing** | 1,120 | 2,331,750 | 2,331,647 | **103** | **FAIL** |

Plumbing is 3 pcs over. The check will go red on its first Plumbing run for a gap that §A2 documents as normal.

**And the comment has the numbers backwards:**

> *"…diverges from the item-level Math.round sum by at most ~100 pcs for a 3,636-item PTMT plan or ~39 pcs for a 1,120-item Plumbing plan"*

Measured is the reverse: PTMT (3,636 items) → **39**; Plumbing (1,120 items) → **103**. The larger item count produced the *smaller* gap. That inversion is very likely how the 100 threshold got set below the observed Plumbing value.

There is also an inconsistency worth resolving while you're in here: the **MISMATCH marker uses ±200** for a structurally identical float-accumulation gap (`grandOrigComputed` vs `frozenPlanGrandMax`), while these checks use ±100. Two thresholds for the same class of quantity, one of which is already exceeded.

**Suggested — and deliberately not "just raise it to 200":**
1. Correct the comment to the measured values, per segment.
2. Record both measured divergences in `LOGIC_PTMT §A2` and `LOGIC_PLUMBING §P1` — right now the only place PTMT 39 / Plumbing 103 appear is my reports.
3. Then set the band **above the observed maximum with stated margin**, and use the same number as the MISMATCH tolerance unless there's a reason they differ. Deriving it from the measurements and writing down the derivation is the difference between a calibrated band and a loosened check.
4. If the band moves to 200, note in the tolerance string which observation set it — otherwise the next person sees a round number with no provenance and the §7 lesson-4 question ("was this loosened to go green?") has no answer.

## Minor

`buildCorrectiveDetailExcel`'s export is documented on the PATCH route rather than at the export site. A reader who greps the function definition won't find the explanation. One line above `export async function` would.

---

## Suggested reply

> Count is 8 — both divergence checks restored and asserted, with §A2 in the tolerance strings. The PATCH gating is stronger than I proposed: `requireApiKey` → 400 → **403 on `NODE_ENV=production`** → 404 → **409 on pinned** (notes still allowed), with `frozenOld`/`frozenNew` logged. The production block makes the field untouchable where it matters rather than merely authenticated.
>
> **One problem: check 7 will fail on Plumbing immediately.** The threshold is ≤100, and the measured Plumbing divergence is **103** (`detailOrigHeader` 2,331,750 vs `storedOrig` 2,331,647). PTMT is 39. So the check goes red on its first Plumbing run for a gap §A2 documents as normal.
>
> The comment also has the numbers reversed — it says *"~100 pcs for a 3,636-item PTMT plan or ~39 pcs for a 1,120-item Plumbing plan"*, but measured it's PTMT 39 (3,636 items) and Plumbing 103 (1,120 items); the larger item count gave the smaller gap. That inversion is probably how the threshold ended up below the observed value.
>
> Please don't just raise it to 200. Instead: correct the comment to the measured values per segment; record PTMT 39 and Plumbing 103 in `§A2` and `§P1` (they currently exist only in my reports); then set the band above the observed maximum with the margin stated, and reconcile it with the **±200** the MISMATCH marker already uses for a structurally identical gap — two thresholds for the same class of quantity, one already exceeded. Put the observation that set the band in the tolerance string, so the next reader can tell a calibrated band from a loosened check.
>
> Two smaller things: **`requireApiKey` is now duplicated** at `corrective.ts:14` and `plant-live.ts:18` — worth hoisting to shared middleware before the copies drift. And please **confirm `NODE_ENV=production` is actually set on the Replit deployment** — nothing in `package.json`, `.replit` or the start script sets it, and if it's unset the 403 never fires and the Bearer token is the only protection left. A guard keyed on an unset variable is the §7 silent-default failure mode.