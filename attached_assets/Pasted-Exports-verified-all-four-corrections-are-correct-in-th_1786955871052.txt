Exports verified — all four corrections are correct in the files. Header vs table now reconciles to 0 on both Original and Revised, both segments. Item counts read row 2 → first blank are exactly 1,120 per Plumbing category and 3,636 for PTMT, in both Detail and Standard. Cap/Day, Feasible and Shortfall are None on every item row and appear only on the KPI row, which sits last after a blank, with the AGRI note after it. NOT_STARTED | NO_DEMONSTRATED_CAPACITY appears on exactly the three Solvent KPI rows (AGRI Solvent correctly has neither, since plan = 0), ZERO_CAP_WITH_PRODUCTION is correctly absent, and the reconciliation note matches the data. All 19 categories pass feasible = cap × 12, remaining = plan − produced, shortfall = max(remaining − feasible, 0) and item-sum = plan, no tolerance.

One thing to change: the MISMATCH marker can't detect anything. run.originalMonthTotal is a column on corrective_plan_runs — the corrective's own stored total, computed from the same item snapshot as grandOrigComputed. So the citation is showing two roundings of one number; they can only ever differ by rounding, and with a strict !== and no tolerance the marker is permanently on. 39 and 103 across 3,636 and 1,120 items are exactly the 0–100 band your memory note describes.

The real cross-check is against the frozen run: Σ productionPlan from plan_run_results for planRunId. The route already computes it as resolvedPlanRunGrandMax (corrective.ts ~:147) for BASELINE_INTEGRITY_ERROR — persist it on the corrective run and cite that instead: #20 (plan run #20: 617,711 pcs · corrective baseline: 617,750 pcs), with ⚠ MISMATCH only beyond ±200. Then the marker means "this corrective no longer matches its frozen baseline", which is worth seeing.

Also for the docs: the Original header is now 617,750 (item-sum), NC20b asserts 617,711 (stored), and the reference golden is 617,710 (dev #21). All three are correct on different bases — please record the table in LOGIC_PTMT §A7 so the next reader doesn't read a basis difference as drift.