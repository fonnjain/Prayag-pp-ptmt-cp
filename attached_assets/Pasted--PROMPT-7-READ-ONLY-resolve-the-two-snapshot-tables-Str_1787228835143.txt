# PROMPT 7 — READ-ONLY: resolve the two snapshot tables

**Strictly read-only.** No writes, no seeding, no migrations, no schema changes, no code changes, no captures, no deletes — in dev or production. If answering anything below appears to require a write, stop and say so.

The June production seed stays blocked until this is settled.

---

## What I have already established from the code

Stating it so you can confirm or correct rather than re-derive:

**Two tables exist.**

| Table | Key | Segment-aware | Provenance columns |
|---|---|---|---|
| `plant_monitoring_snapshots` | `month` (**PK**) | **no** | none — `month`, `plan_run_id`, four JSONB blobs, `captured_at` |
| `plant_month_snapshots` | `(month, segment)` unique | yes | `closed_at`, `captured_at`, `captured_commit_sha`, `backfilled`, `plan_status`, `plan_status_reason`, `plan_evidence_json` |

**Only two files reference `plant_month_snapshots`:**

- `scripts/seed-june-actuals-only.ts` — writes it (`:86`)
- `lib/plan-vs-actual-engine.ts` — reads it (`:1195–1203`)

**`plant-monitoring.ts` — the closed-month path — references it zero times.** It uses `plantMonitoringSnapshotsTable` throughout (`:337`, `:353`).

So on the evidence I can see: the freeze feature's provenance columns are populated by a one-time script and read only by the Plan-vs-Actual engine, while the monitoring path that actually runs on a closed-month read uses a different, segment-blind table.

**Please confirm or correct each of those statements before going further.** If I have this wrong, everything below changes.

---

## The questions

### 1. What is actually in `plant_monitoring_snapshots` in production?

The previous report queried `plant_month_snapshots` and found it empty — but that is not the table the deployed code writes.

All rows, with: `month`, `plan_run_id`, `captured_at`, and the produced total extracted from `bundle_json`.

For any row found, compare that produced total against the true source for the month, both segments. **A row for 2026-06 showing 920,507 or 1,444,669 was captured with the Sunday drop.** Report the delta; do not correct it.

### 2. Trace a closed-month read end to end

For `GET /api/plant/bundle?month=2026-06&segment=PTMT` against a CLOSED month, report the actual sequence: which table is consulted, whether a snapshot is written if absent, and which table receives it.

Then state plainly: **is `plant_month_snapshots` read or written on any live request path?** My reading is no — only by a one-time script and the Plan-vs-Actual engine. If that is right, the freeze feature is not deployed in the sense that matters, whatever the migrations say.

### 3. How does a segment-blind table hold two segments?

`plant_monitoring_snapshots.month` is the **primary key**, and there is a single `plan_run_id` column. PTMT and Plumbing for 2026-06 cannot both exist.

Which is it:

- PTMT-only by design, with Plumbing closed months handled elsewhere or not at all?
- Or are both segments writing to it, so one silently overwrites the other?

If the second, that is a live data-loss path on every closed month, and it matters more than anything else in this prompt.

### 4. Is "June is frozen in dev" actually true?

`seed-june-actuals-only.ts:86` writes `plant_month_snapshots`. If the monitoring path reads `plant_monitoring_snapshots`, then dev's June rows are in a table the monitoring UI never consults — and June would still be recomputed live there.

Check in dev: does `GET /api/plant/bundle?month=2026-06` return the seeded actuals-only payload with `attainmentPct: null`, or does it recompute? That is the test of whether the seed did anything.

### 5. What reads the provenance columns?

`plan-vs-actual-engine.ts:1195–1203` selects `planStatus`, `planStatusReason`, `planEvidenceJson`. Report which endpoint that serves and whether it is reachable today.

If the Plan-vs-Actual report is the only consumer, then `captured_commit_sha` and `backfilled` — the fields built specifically so a frozen month's provenance is auditable — are read by nothing at all. Please confirm.

### 6. `plant_configs` for 2026-07

Production has `working_days = 25`, `snapshot_date = 2026-07-05`. That stale 5 July cursor is what produced the *"4/25 working days elapsed"* screen this workstream began with.

With observed-working-days now deployed, report what July resolves to today: `workingDays`, `workingDaysSource`, `elapsed`, and whether the configured 25 still overrides the observed count. Do not change it — I want to know whether the override is now fighting the fix.

---

## Report format

Plain tables, plus a direct answer to each numbered question. Where a read fails, report the error verbatim rather than substituting zero or "none".

**Nothing is to be written, published, seeded or migrated on the basis of this report.** The point is to find out what the deployed system actually does before we change anything else.