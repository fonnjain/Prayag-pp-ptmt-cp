Environment gate confirmed working — ep-orange-tree-…neon.tech vs helium, mode printed. Three things:

1. Deployed commit: (unknown) means production predates d1af19c — i.e. every commit from this session. No migration 022, no corrective export fixes, no hoisted aggregate, no /healthz commitSha, no GET /plan/runs normalisation. So the four corrective exports I verified earlier today came from dev, not production — the header/table reconciliation, the 1,120 / 3,636 item counts, the KPI rows, the Solvent flags are all confirmed for the dev build only. Please record that against those results in the handover; "the exports are correct" without an environment attached is the same ambiguity that cost five rounds on the baseline ids. Baseline resolution itself is probably right in production since #20/#21 were created there via API and auto-select is data-driven — but that's an inference until NC20a/NC21a run.

2. process.exit(1) at verify-plumbing-plan.ts:309 is why nothing downstream ran. The Plumbing validate call is the most Sheets-dependent section in the suite, and it aborts 2,190 lines of checks — several with no Sheets dependency at all: NC20f, NC22a, NC22b, NC22c and the AGRI item-count block are DB-only (GET /plan/runs, GET /corrective/runs, GET /corrective/runs/:id, POST /plan/runs rejection paths, and the export which reads persisted items). Those six could have reported today. NC20a–c/NC21a–c/NC20e/NC21e do genuinely touch Sheets, since the engine reads production actuals at corrective-engine.ts:387–390. Replace that process.exit(1) with "record the section as failed and continue" — the final process.exit(anyFail ? 1 : 0) at :2493 already gives the right status, and bundle-freshness at :281 is already the correct pattern. Also consider moving the Sheets-free checks ahead of the Sheets-dependent ones, so a quota failure never costs you the DB-backed results.

3. Bundle freshness still can't see production. It compares local dist/index.cjs mtime against local git log. Now that /healthz reports commitSha, add the real check: assert the deployed SHA equals git rev-parse HEAD. Today that would have surfaced "production is at least 6 commits behind, no commitSha field" as a named failure instead of an (unknown) line in the header.